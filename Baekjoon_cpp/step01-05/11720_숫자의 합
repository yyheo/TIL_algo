#include <iostream>
using namespace std;

// 11720 숫자의 합
int main() {
	int n, sum = 0;
	cin >> n;
	char num[100] = { 0 };
	for (int i = 0; i < n; i++) {
		cin >> num[i];
		sum += num[i] - '0';
	}
	cout << sum;
}
